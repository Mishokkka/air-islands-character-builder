diff --git a/foundry-module/scripts/core.mjs b/foundry-module/scripts/core.mjs
index a1bb209..7e36aa4 100644
--- a/foundry-module/scripts/core.mjs
+++ b/foundry-module/scripts/core.mjs
@@ -5,3 +5,6 @@ export function sanitizeEmbeddedItem(snapshot, rank = null, foundryGeneration =
   delete item.flags["scene-packer"];
-  if (rank !== null && item.system) item.system.rank = String(rank);
+  if (rank !== null && item.system) {
+    const numericRank = Number(rank);
+    if (Number.isFinite(numericRank)) item.system.rank = numericRank;
+  }
   const generation = Number(foundryGeneration);
@@ -18,3 +21,2 @@ export function characterToQuickAccessBiographyProfile(character, rules) {
       id: String(entry?.id ?? `rumor-${position + 1}`),
-      name: String(entry?.name ?? entry?.characterName ?? entry?.source ?? ""),
       text: String(entry?.text ?? ""),
diff --git a/foundry-module/scripts/main.mjs b/foundry-module/scripts/main.mjs
index aa274fa..efe5cfd 100644
--- a/foundry-module/scripts/main.mjs
+++ b/foundry-module/scripts/main.mjs
@@ -2,2 +2,3 @@ import { validateCharacter, characterToActorData, characterToQuickAccessBiograph
 import { isZip, readZip, decodeText, createZip } from "./zip.mjs";
+import { applyQuickAccessImport } from "./quick-access-bridge.mjs";
 
@@ -26,29 +27,9 @@ async function applyQuickAccessIntegration(actor, character, rules) {
   const quickAccess = game.modules.get("fbl-quick-access")?.api;
-  try {
-    if (typeof quickAccess?.saveReputationEntries === "function") {
-      await quickAccess.saveReputationEntries(actor, reputationEntries, { render: false });
-    } else {
-      await actor.update({
-        "flags.fbl-quick-access.reputationEntries": reputationEntries,
-        "system.bio.reputation.value": reputationEntries.reduce((sum, entry) => sum + entry.amount, 0)
-      }, { render: false });
-    }
-    if (typeof quickAccess?.saveWillpowerTalents === "function") {
-      await quickAccess.saveWillpowerTalents(actor, selection, { render: false });
-    } else {
-      await actor.update({
-        "flags.fbl-quick-access.willpowerTalents": selection
-      }, { render: false });
-    }
-    if (typeof quickAccess?.saveBiographyProfile === "function") {
-      await quickAccess.saveBiographyProfile(actor, biographyProfile, { render: false });
-    } else {
-      await actor.update({
-        "flags.fbl-quick-access.biographyProfile": biographyProfile
-      }, { render: false });
-    }
-  } catch (error) {
-    console.warn(`${MODULE_ID} | Quick Access integration failed`, error);
-    ui.notifications?.warn?.("Персонаж импортирован, но данные Quick Access не удалось записать автоматически.");
-  }
+  await applyQuickAccessImport({
+    actor,
+    quickAccess,
+    reputationEntries,
+    selection,
+    biographyProfile
+  });
 }
diff --git a/shared/core.mjs b/shared/core.mjs
index a1bb209..7e36aa4 100644
--- a/shared/core.mjs
+++ b/shared/core.mjs
@@ -5,3 +5,6 @@ export function sanitizeEmbeddedItem(snapshot, rank = null, foundryGeneration =
   delete item.flags["scene-packer"];
-  if (rank !== null && item.system) item.system.rank = String(rank);
+  if (rank !== null && item.system) {
+    const numericRank = Number(rank);
+    if (Number.isFinite(numericRank)) item.system.rank = numericRank;
+  }
   const generation = Number(foundryGeneration);
@@ -18,3 +21,2 @@ export function characterToQuickAccessBiographyProfile(character, rules) {
       id: String(entry?.id ?? `rumor-${position + 1}`),
-      name: String(entry?.name ?? entry?.characterName ?? entry?.source ?? ""),
       text: String(entry?.text ?? ""),
diff --git a/tools/test-engine.mjs b/tools/test-engine.mjs
index 654ba10..88503ae 100644
--- a/tools/test-engine.mjs
+++ b/tools/test-engine.mjs
@@ -2,3 +2,5 @@ const quickAccessBiography = characterToQuickAccessBiographyProfile(sample, rule
 assert.equal(quickAccessBiography.languages[0].cost, 0, "Родной базовый язык не должен повторно стоить очки");
-assert.equal(quickAccessBiography.rumors[0].name, "Сестра");
+assert.equal(quickAccessBiography.rumors[0].text, sample.biography.rumors[0].text);
+assert.equal(quickAccessBiography.rumors[0].truth, sample.biography.rumors[0].truth);
+assert.equal("name" in quickAccessBiography.rumors[0], false);
 assert.equal(rules.catalogs.talents.package, "world.talents");
@@ -6,3 +8,5 @@ assert.equal(rules.catalogs.talents.package, "world.talents");
 assert.equal(actorData.flags["fbl-quick-access"].biographyProfile.identity.issuingCountry, "Сиростьен");
-assert.equal(actorData.flags["fbl-quick-access"].biographyProfile.rumors[0].name, "Сестра");
+assert.equal(actorData.flags["fbl-quick-access"].biographyProfile.rumors[0].text, sample.biography.rumors[0].text);
+assert.equal(actorData.flags["fbl-quick-access"].biographyProfile.rumors[0].truth, sample.biography.rumors[0].truth);
+assert.equal("name" in actorData.flags["fbl-quick-access"].biographyProfile.rumors[0], false);
 assert.match(actorData.system.bio.note.value, /Сдержанный бывший стражник/u);
@@ -10,3 +14,4 @@ assert.match(actorData.system.bio.note.value, /Сдержанный бывший
 const effectV14 = sanitizeEmbeddedItem(effectSnapshotV13, 2, 14);
-assert.equal(effectV14.system.rank, "2");
+assert.equal(effectV14.system.rank, 2);
+assert.equal(typeof effectV14.system.rank, "number");
 assert.equal(effectV14.effects[0].changes, undefined);
diff --git a/tools/test-foundry-compat.mjs b/tools/test-foundry-compat.mjs
index ba79b08..8a38937 100644
--- a/tools/test-foundry-compat.mjs
+++ b/tools/test-foundry-compat.mjs
@@ -3,2 +3,4 @@ const main = fs.readFileSync(path.join(root, "foundry-module/scripts/main.mjs"),
 const core = fs.readFileSync(path.join(root, "shared/core.mjs"), "utf8");
+const foundryCore = fs.readFileSync(path.join(root, "foundry-module/scripts/core.mjs"), "utf8");
+const quickAccessBridge = fs.readFileSync(path.join(root, "foundry-module/scripts/quick-access-bridge.mjs"), "utf8");
 const importerCss = fs.readFileSync(path.join(root, "foundry-module/styles/importer.css"), "utf8");
@@ -7,7 +9,13 @@ assert.equal(manifest.compatibility.minimum, "13");
 assert.match(main, /applyQuickAccessIntegration/u);
-assert.match(main, /saveReputationEntries/u);
-assert.match(main, /saveWillpowerTalents/u);
-assert.match(main, /saveBiographyProfile/u);
+assert.match(main, /applyQuickAccessImport/u);
 assert.match(main, /characterToQuickAccessBiographyProfile/u);
-assert.match(main, /flags\.fbl-quick-access\.willpowerTalents/u);
+assert.match(quickAccessBridge, /saveReputationEntries/u);
+assert.match(quickAccessBridge, /saveWillpowerTalents/u);
+assert.match(quickAccessBridge, /saveBiographyProfile/u);
+assert.match(quickAccessBridge, /savePilgrimCardProfile/u);
+assert.match(quickAccessBridge, /flags\.fbl-quick-access\.willpowerTalents/u);
+assert.doesNotMatch(quickAccessBridge, /api\s*\[\s*methodName\s*\]\s*=/u);
+assert.doesNotMatch(quickAccessBridge, /fblQuickAccess\.apiReady/u);
+assert.match(core, /item\.system\.rank = numericRank/u);
+assert.match(foundryCore, /item\.system\.rank = numericRank/u);
 assert.match(core, /"fbl-quick-access"/u);
